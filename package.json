{
  "name": "crowdfunding-nft",
  "workspaces": [
    "hardhat",
    "nextjs"
  ],
  "scripts": {
    "node": "npm run node --workspace=hardhat",
    "compile": "npm run compile --workspace=hardhat",
    "deploy": "npm run deploy --workspace=hardhat",
    "deploy:init": "npm run deploy:init --workspace=hardhat",
    "deploy:sepolia": "npm run deploy:sepolia --workspace=hardhat",
    "generate": "npm run generate --workspace=nextjs",
    "dev": "npm run dev --workspace=nextjs",
    "build": "npm run build --workspace=nextjs",
    "start": "npm run start --workspace=nextjs"
  }
}
